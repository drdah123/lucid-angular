export interface Car {
  name: string;
  desc: string;
  image: string;
}

export interface ComparCard {
  name: string;
  wheel: string;
  color: string;
}

export interface wheel {
  name: string;
  model: string;
  image: string;
}
